{
  "name": "linkbruh",
  "version": "1.0.0",
  "description": "Discord bot that rewrites X/Twitter and Instagram links to embed-proxy domains so they play inline.",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.18.0"
  },
  "dependencies": {
    "discord.js": "^14.16.0"
  },
  "license": "MIT"
}
