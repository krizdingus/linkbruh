# Copy to .env for local runs. On Railway, set these in the service variables.

# Bot token from the Discord Developer Portal (Bot tab).
DISCORD_TOKEN=

# Application (client) ID — used to build the invite URL. Not required at runtime.
CLIENT_ID=
